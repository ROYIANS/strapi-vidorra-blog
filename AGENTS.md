# AGENTS.md

本文件用于沉淀本仓库协作开发中的通用执行规范。

## 1. 国际化（i18n）规范

1. 所有面向用户的文案必须走 i18n key，不允许硬编码在组件中。
2. 新增页面/组件时，默认同时补齐 `zh-CN` 与 `en-US` 文案。
3. 文案 key 命名保持稳定、语义化，禁止按页面临时命名。
4. 涉及动态菜单、标题、按钮文案时，优先在渲染阶段取值，避免不可预期的静态缓存。
5. 语言切换能力必须可持久化（至少 localStorage/cookie 其一，推荐双写）。

## 2. 编码与文件读写规范

1. 全仓库文本文件统一使用 UTF-8（建议 UTF-8 with LF）。
2. 使用脚本/命令写文件时，必须显式指定编码（例如 PowerShell `-Encoding UTF8`）。
3. 涉及中文文件改动后，必须做最小验证：
   - 页面渲染是否出现乱码。
   - 文案 key 是否正常命中。
4. 不要在不确定编码的前提下批量替换或重写 i18n 文件。

## 3. 开发验证门禁（最小）

涉及 `apps/server` 或 `apps/admin` 改动时，提交前至少执行：

1. `pnpm --filter server test`
2. `pnpm --filter server build`
3. `pnpm --filter admin typecheck`
4. `pnpm --filter admin build`

若任一步失败，不得宣称“已完成”。

## 4. 调试与修复原则

1. 先定位根因，再实施修复，禁止猜测式补丁。
2. 修复认证/权限/用户同步问题时，优先补充回归测试。
3. 新增兼容逻辑前先确认是否仍处开发期；开发期可优先清理冗余，减少历史包袱。

## 5. 文档沉淀要求

1. 每次完成一个阶段性目标，在 `docs/` 新增简要复盘文档。
2. 文档至少包含：完成项、验证证据、风险项、下一步计划。
